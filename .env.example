PORT=3001
APP_URL=http://localhost:3000
ALLOWED_ORIGIN=http://localhost:4000
JWT_SECRET=local-jwt-secret

# MongoDB Configuration
MONGO_USERNAME=root
MONGO_PASSWORD=example
MONGO_DATABASE=clockwise_ai
MONGO_HOST=mongodb
MONGO_PORT=27017

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-asd
OPENAI_VECTOR_STORE_ID=vs_asd
OPENAI_MODEL=gpt-5-mini
OPENAI_HELPER_MODEL=gpt-4o-mini

# Context Optimization
MAX_CONVERSATION_ITEMS=10                                   # Number of meaningful conversation items to keep
TOOL_RESPONSE_ARRAY_THRESHOLD=5                             # Start field extraction for arrays larger than this
TOOL_RESPONSE_MAX_DEPTH=3                                   # Max recursion depth for nested object processing
TOOL_RESPONSE_MAX_STRING_LENGTH=1000                        # Max string length before truncation
TOOL_RESPONSE_KEY_FIELDS=id,name,title,customTitle,userId   # Key fields to extract from objects